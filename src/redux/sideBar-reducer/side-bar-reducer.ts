const initialState = {}
const sideBarReducer = (state: any = initialState, action: any) => {
    switch (action.type) {
        case 'XXX': {
            return state;
        }
        default:
            return state
    }
}

export default sideBarReducer;